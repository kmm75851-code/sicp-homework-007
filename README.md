# Homework 007 — Sequences, Strings & Generators

SICP Course | Урок 008

## Файлы

| Файл | Тема | Функций |
|------|------|---------|
| `sequences.py` | map / filter / reduce | 8 |
| `strings.py` | Строки как последовательности | 8 |
| `generators.py` | Ленивые генераторы | 7 |

## Запуск тестов

```bash
pytest tests/ -v
```

## Ограничения

- `sequences.py` — только `map`, `filter`, `reduce`. Никаких `for`/`while`.
- `generators.py` — функции должны быть генераторами (использовать `yield`).
